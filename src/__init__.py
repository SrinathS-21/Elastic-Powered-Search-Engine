"""Pepagora search API package.

This file intentionally exists to make `src` a proper Python package so relative
imports are stable across entrypoints (`uvicorn src.main:app`, `python -m src.main`,
and test runners).
"""

